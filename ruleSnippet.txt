rule ""
	when Answer(
		d:dialog, d.getQuestion == ""
		&& getDialogAnswer() == ""
	)
	then
	Dialog tempDialog = new Dialog(
		"",
		new String[] {""}
	);
	String userAnswer = window.askUser(tempDialog.getQuestion(), tempDialog.getAnswers());
	insert(new Answer(tempDialog,userAnswer));
end